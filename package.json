{
  "name": "action-find-linear-issue",
  "version": "1.3",
  "private": true,
  "description": "Finds a Linear Issue from your pull request's branch, title, or description",
  "keywords": [],
  "license": "MIT",
  "author": "nrpx",
  "main": "src/main.ts",
  "scripts": {
    "all": "npm run format && npm run build && npm run package",
    "build": "tsc",
    "format": "prettier --write **/*.ts",
    "package": "ncc build --license licenses.txt"
  },
  "dependencies": {
    "@actions/core": "^1.8.0",
    "@actions/github": "^5.0.1",
    "@linear/sdk": "^10.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vercel/ncc": "^0.33.4",
    "prettier": "^2.6.2",
    "typescript": "4.6.4"
  }
}
